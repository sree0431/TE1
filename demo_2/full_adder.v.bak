module full_adder (sum, cout, A, B, cin);
	output sum, cout;
	input A, B, cin;
	
	wire w1, w2, w3, w4;
	
	xor (w1, A, B);
	xor (sum, w1, cin);
	
	and (w2, A, B);
	and (w3, B, cin);
	and (w4, cin, A);
	or  (cout, w2, w3, w4);
	
endmodule 